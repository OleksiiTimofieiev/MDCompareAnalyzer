+ configs via json for acceptable mismatch
+ configs via json to write results to file
+ configs via json to write results to excel

! think about concurrency => array of different containers for parsing
! menu in loop

second iteration:
- load not all sheets as option

+ read json in constructor(during start)
+ file for analysis in command line
+ usage line
- exceptions formatting acceptance
- no spaces in the name of file, if exception during open file by openpxl ->
- menu func
- select FIDs with not acceptable mismatches

""
"asdfasdf "

соответствие риков = тождественны ли множества риков
тип несоответствия в файл/на консоль










- all diff with all criteria, types of mismatch, RIC
- options for fids;
- one or all differencies;
- check the type of mismatch
- sort types of mismatches for FID
- write results to file
- выгрузить все по рику
- print necessary FIDs according to RIC
- add exception/exclude exception un runtime or fid
- types of errors
- statistics and vizualization
- enum for the fields
- if all acceptable, add to exceptions
- write all data for RIC in separate file

